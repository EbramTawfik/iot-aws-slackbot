{
  "name": "aws-slackbot",
  "version": "1.0.0",
  "description": "Handles pushing messages out to a Slack channel.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "slackbots": "^1.2.0"
  }
}
